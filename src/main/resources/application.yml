spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cadastro_clientes?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: P4ssw0rd
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true